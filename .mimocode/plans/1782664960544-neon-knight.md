# Plan: Full Mobile Fix

## Files to modify
1. `D:\Project\Porftolio\css\style.css`
2. `D:\Project\Porftolio\js\main.js`

---

## CSS Changes (`css/style.css`)

### 1. Add touch/hover reset (before media queries, after line 1117)
Add `@media (hover: none)` block to disable sticky hover states on touch devices:
```css
@media (hover: none) {
    .nav-icon:hover, .nav-icon:hover .nav-tooltip,
    .section-link:hover, .anim-btn:hover,
    .anim-btn:hover .anim-btn-text, .anim-btn:hover .anim-btn-clone,
    .social-icon:hover, .info-row a:hover,
    .service-card:hover, .timeline-content:hover,
    .education-card:hover, .project-row:hover,
    .project-row:hover .project-arrow,
    .contact-col a:hover { all: unset; }
    .nav-tooltip { display: none !important; }
}
```

### 2. Expand 480px breakpoint (line 1262-1281) with additional rules
Add inside `@media (max-width: 480px)`:
- `.section-label` → `font-size: 1.5rem; margin-bottom: 24px;`
- `.hero` → `padding: 80px 16px 40px;`
- `.contact-cta` → `padding: 40px 0 30px;`
- `.contact-grid` → `gap: 24px;`
- `.info-row` → `flex-direction: column; gap: 4px;` (prevents email overflow)
- `.blog-empty` → `padding: 30px 0;`

### 3. Fix touch target sizes
- `.faq-toggle`: increase from `32px` to `44px` (line 990-991)
- `.contact-col a`: change `padding: 4px 0` to `padding: 10px 0` (line 1066)

### 4. Add body scroll lock class
```css
body.menu-open { overflow: hidden; }
```

### 5. Add `prefers-reduced-motion` query (end of file)
```css
@media (prefers-reduced-motion: reduce) {
    *, *::before, *::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
    .marquee-track { animation: none; }
}
```

---

## JS Changes (`js/main.js`)

### 1. Mobile nav scroll lock (line 62-65)
Toggle `body.classList.toggle('menu-open')` when hamburger is clicked:
```js
hamburger.addEventListener('click', () => {
    hamburger.classList.toggle('active');
    navMenu.classList.toggle('active');
    document.body.classList.toggle('menu-open');
});
```

### 2. Close menu on Escape key (after line 72)
```js
document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && navMenu.classList.contains('active')) {
        hamburger.classList.remove('active');
        navMenu.classList.remove('active');
        document.body.classList.remove('menu-open');
    }
});
```

### 3. Disable canvas on mobile (line 156-168)
Add viewport check to skip canvas init on small screens:
```js
const isMobile = window.innerWidth <= 768;
if (canvas && !isMobile) {
    // existing canvas code...
}
```

---

## Verification
1. Open `index.html` in browser, resize to 320px/375px/480px widths
2. Verify hamburger menu opens/closes, body scroll is locked
3. Verify hover states don't stick on mobile (test with Chrome DevTools device mode)
4. Verify FAQ toggle and contact links are tappable (44px+ targets)
5. Verify email text doesn't overflow at narrow widths
6. Verify canvas animation is disabled on mobile
7. Verify Escape key closes mobile menu
